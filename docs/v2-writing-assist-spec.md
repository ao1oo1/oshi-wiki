# Oshi-Wiki v2：AI執筆補助機能

## v2の目的

v2では、AI本文生成は実装せず、AIに貼り付けるためのプロンプト作成・保存・コピー機能を実装する。

## v2で実装すること

- 一般ユーザー登録
- 一般ユーザーログイン
- 一般ユーザー用マイページ
- オリジナルキャラクター登録
- オリジナルキャラクター関係性登録
- v1の作品・キャラクター・関係性データ参照
- v1データとv2データを組み合わせたプロンプト作成
- プロンプト保存
- プロンプトコピー

## v2では実装しないこと

- OpenAI API連携
- Ollama連携
- サイト内AI本文生成
- AIによる自動執筆

## DB使用制限

1ユーザーあたり：

- オリジナルキャラクター：最大30件
- 関係性：最大100件
- プロンプト管理：最大50件
- 1プロンプト本文：最大20,000文字
- あらすじ：最大5,000文字
- 各備考：最大2,000〜5,000文字

## 実装フェーズ

1. v2ブランチ作成・v1タグ確認
2. 一般ユーザー登録/ログイン/マイページ
3. DB使用制限の共通定数作成
4. オリジナルキャラクターDB/CRUD
5. 関係性DB/CRUD
6. プロンプト作成DB/CRUD
7. v1データ抽出
8. プロンプト生成ロジック
9. コピー機能・UI調整・v2タグ保存

### 最高管理者の制限

v2のDB使用制限は一般ユーザー向けの制限とする。

最高管理者権限保持者は、以下の制限を受けない。

- オリジナルキャラクター登録数
- 関係性登録数
- プロンプト管理数
- プロンプト本文文字数
- あらすじ文字数
- 備考文字数

実装上は、`App\Support\WritingAssistLimits` でユーザーを受け取り、最高管理者の場合は `null` を返す。  
`null` は「制限なし」として扱う。

## Phase 6：プロンプト管理

AIに直接本文生成させる機能は実装せず、ユーザーがAIに貼り付けるためのプロンプトを保存・編集・コピーできるようにする。

### 保存項目

- タイトル
- カテゴリ
- 用途
- あらすじ・前提
- プロンプト本文
- 備考
- 状態

### 制限

一般ユーザーはプロンプト管理を最大50件まで保存できる。  
最高管理者は保存件数・文字数ともに制限なし。

### コピー機能

詳細画面に「本文をコピー」ボタンを設置し、作成済みプロンプト本文をクリップボードへコピーできるようにする。

## Phase 7：登録済みデータを使ったプロンプト精度強化

プロンプト管理作成時、選択されたキャラクターの登録済み情報をプロンプト本文へ自動反映する。

### 反映対象

- v1 作品キャラクター情報
- v2 オリジナルキャラクター情報
- v1 作品キャラクター同士の関係性
- v2で登録した関係性

### 目的

ユーザーが都度キャラクター設定を手入力しなくても、登録済みデータベースを元にAIへ渡すプロンプトを作成できるようにする。

## Phase 8：プロンプト作成画面の使いやすさ改善

プロンプト作成画面を、項目ごとに入力しやすいステップ形式へ改善する。

### 改善内容

- 作品選択に応じて、作品キャラクターを絞り込み表示する
- オリジナルキャラクターと作品キャラクターを分けて表示する
- 登場人物選択を複数選択ボックスからチェックボックス形式へ変更する
- 作風「その他」選択時のみ自由入力欄を表示する
- ジャンル「その他」選択時のみ自由入力欄を表示する
- 入力補助文を追加する

## Phase 9：プロンプト生成結果の見やすさ改善・コピー機能強化

プロンプト管理詳細画面を改善し、生成条件・あらすじ・起承転結・生成本文を確認しやすくする。

### 改善内容

- 生成条件をカード形式で表示
- あらすじ、起承転結、備考を整理して表示
- 生成されたプロンプト本文を大きいテキストエリアで表示
- 全文コピー、本文コピー、本文全選択ボタンを設置
- コピー成功メッセージを表示

## Phase 10：下書き保存・複製機能

プロンプト管理に下書き保存と複製機能を追加する。

### 下書き保存

プロンプト作成・編集画面に「下書きにする」ボタンを設置する。  
下書きにするした場合、`status` は `draft` とする。

### 複製

一覧画面・詳細画面から作成済みプロンプトを複製できるようにする。  
複製後のプロンプトは以下の状態で作成する。

- タイトル：元タイトル + 「のコピー」
- 所有者：現在ログイン中のユーザー
- 状態：下書き
- 複製後は編集画面へ遷移する

## Phase 11：プロンプト管理の検索・絞り込み機能

プロンプト管理一覧に検索・絞り込み機能を追加する。

### 検索条件

- キーワード
- 作品種別
- 作品名
- 作風
- ジャンル
- 状態

### 備考

検索条件はページネーション移動時も維持する。

## Phase 12：プロンプト利用履歴・最終利用日時

プロンプト詳細画面でコピー操作を行った場合、利用履歴を更新する。

### 記録項目

- 利用回数
- 最終利用日時

### 動作

「全文コピー」「本文コピー」ボタンを押してコピーに成功した場合、非同期通信で利用回数と最終利用日時を更新する。  
複製されたプロンプトは利用回数0回、最終利用日時なしで作成する。

## Phase 13：プロンプト管理の並び替え・よく使う順表示

プロンプト管理一覧に並び替え機能を追加する。

### 並び替え条件

- 新しい順
- 古い順
- 更新が新しい順
- よく使う順
- 最近使った順
- タイトル昇順
- タイトル降順

検索条件と並び替え条件はページネーション時も維持する。

## Phase 14：プロンプト作成画面のプレビュー機能

プロンプト作成・編集画面にプレビュー生成機能を追加する。

### 概要

保存前に、入力中の条件から生成されるプロンプト本文を確認できるようにする。

### 機能

- プレビュー生成ボタン
- プレビュー本文表示
- プレビュー本文コピー
- 保存せずに生成結果を確認可能

### 備考

プレビュー生成は保存処理を行わず、現在入力されている内容をもとに一時的にプロンプト本文を生成する。

## Phase 15：一般会員ダッシュボード案内カード追加

一般会員向けダッシュボードに、以下の案内カードを追加する。

### 追加内容

- はじめての方へ
- 使い方ガイドへのリンク
- データ登録リクエストフォームへの導線
- コントリビュータ応募への導線

### 使い方ガイド

`/writer/guide` を追加し、以下の手順を説明する。

1. オリジナルキャラクターを登録する
2. 関係性を登録する
3. プロンプトを作成する
4. プレビューで確認する
5. プロンプトをコピーしてChatGPTなどのAIへ貼り付ける

## Phase 15補足：お問い合わせフォームへの導線

データ登録リクエスト、コントリビュータ応募は、新規フォームを作成せず、v1で作成済みのお問い合わせフォームへ誘導する。

### 対象

- データ登録リクエスト
- コントリビュータ応募

### 表示場所

- 一般会員ダッシュボード

## Phase 16：お問い合わせフォームへの種別付き導線

一般会員ダッシュボードの「データ登録リクエスト」「コントリビュータ応募」は、v1で作成済みのお問い合わせフォームへ誘導する。

### URLパラメータ

- `type=data_request`
- `type=contributor`

お問い合わせフォーム側で対応できる場合は、件名や種別表示に自動反映する。

## Phase 17：使い方ガイドの見た目調整と公開ページ導線

`/writer/guide` をカード型レイアウトに調整し、以下の導線を強化する。

### 追加・調整内容

- すぐ始める導線
- STEP形式の概要カード
- 詳細な使い方説明
- AI利用時の注意事項
- データ登録リクエスト・コントリビュータ募集への補足

## Phase 17：使い方ガイドの見た目調整と公開ページ導線

`/writer/guide` をカード型レイアウトに調整し、以下の導線を強化する。

### 追加・調整内容

- すぐ始める導線
- STEP形式の概要カード
- 詳細な使い方説明
- AI利用時の注意事項
- データ登録リクエスト・コントリビュータ募集への補足

## Phase 18：プロンプト管理一覧UI最終調整

`/writer/prompts` の一覧画面をカード型レイアウトに調整する。

### 調整内容

- プロンプト一覧をカード表示に変更
- 検索、絞り込み、並び替えを整理
- 利用回数、最終利用日、更新日を表示
- 詳細、編集、複製、削除の操作ボタンを整理
- 空状態の案内を追加

## Phase 19：オリジナルキャラクター管理一覧UI調整

`/writer/original-characters` の一覧画面をカード型レイアウトに調整する。

### 調整内容

- 登録件数、表示件数を表示
- キャラクター一覧をカード表示に変更
- 主人公、ステータス、性別、年齢をバッジ表示
- 所属、一人称、更新日をカード内に表示
- 詳細、編集、削除の操作ボタンを整理
- 空状態の案内を追加

## Phase 19：オリジナルキャラクター管理一覧UI調整

`/writer/original-characters` の一覧画面をカード型レイアウトに調整する。

### 調整内容

- 登録件数、表示件数を表示
- キャラクター一覧をカード表示に変更
- 主人公、ステータス、性別、年齢をバッジ表示
- 所属、一人称、更新日をカード内に表示
- 詳細、編集、削除の操作ボタンを整理
- 空状態の案内を追加

## Phase 20：関係性管理一覧UI調整

`/writer/original-character-relationships` の一覧画面をカード型レイアウトに調整する。

### 調整内容

- 登録件数、表示件数を表示
- 関係性一覧をカード表示に変更
- From / To のキャラクター表示を強調
- オリジナルキャラ / 作品キャラの区分をバッジ表示
- 呼び方、関係性、更新日を表示
- 詳細、編集、削除の操作ボタンを整理
- 空状態の案内を追加

## Phase 20.5：関係性の複製機能追加

関係性管理に複製機能を追加する。

### 追加内容

- 関係性一覧に複製ボタンを追加
- 関係性詳細画面に複製ボタンを追加
- 複製後は編集画面へ遷移
- 複製データの `user_id` は現在ログイン中のユーザーに設定

## Phase 21：オリジナルキャラクター詳細画面UI調整

`/writer/original-characters/{id}` の詳細画面をカード型レイアウトに調整する。

### 調整内容

- 上部に一覧へ戻る、編集、削除ボタンを横並びで表示
- 主人公、ステータス、性別、年齢をバッジ表示
- 基本情報、話し方、キャラクター設定、プロンプト用メモに分割
- 未入力項目は「未入力」と表示

## Phase 22：プロンプト詳細画面UI統一

`/writer/prompts/{id}` の詳細画面をカード型レイアウトに調整する。

### 調整内容

- 一覧へ戻る、編集、複製、削除ボタンを横並びで表示
- 作成条件、あらすじ、起承転結、備考をカードで整理
- プロンプト本文を大きなtextareaで表示
- 全文コピー、本文コピー、本文全選択ボタンを整理
- コピー時に利用回数と最終利用日を更新

## Phase 23：一般会員のデータ登録上限表示

一般会員向け画面に、現在の登録件数と登録上限を表示する。

### 表示形式

- `10 / 50`
- `10 / 制限なし`

### 対象

- ダッシュボード
- サイドバー
- 使い方ガイド
- オリジナルキャラクター一覧
- 関係性一覧
- プロンプト管理一覧

### 上限

- オリジナルキャラクター：30件
- 関係性：100件
- プロンプト管理：50件
- 最高管理者：制限なし

## Phase 23：入力フォーム画面のUI統一

一般会員向けの入力フォーム画面を、Oshi-Wikiのカード型UIに合わせて調整する。

### 対象

- オリジナルキャラクター新規登録
- オリジナルキャラクター編集
- 関係性新規登録
- 関係性編集
- プロンプト新規作成
- プロンプト編集

### 調整内容

- input / select / textarea の角丸、余白、文字色を統一
- フォーカス時の色をOshi-Wikiのメインカラーに統一
- textarea の高さと行間を調整
- ラベル、説明文、placeholderの見た目を統一
- チェックボックス、ラジオボタンは崩れない範囲で調整
- 既存のプレビュー機能、複製機能は維持

## Phase 24：新規登録・編集フォームの入力項目整理

オリジナルキャラクター登録・編集フォームをセクション形式に整理する。

### 調整内容

- STEP 1：基本情報
- STEP 2：話し方
- STEP 3：キャラクター設定
- STEP 4：プロンプト用メモ
- 必須項目を明示
- 保存ボタンまわりを統一

## Phase 25：関係性フォームの入力項目整理

関係性登録・編集フォームをセクション形式に整理する。

### 調整内容

- STEP 1：関係を作るキャラクターを選ぶ
- STEP 2：呼び方・関係性
- STEP 3：印象・備考
- 必須項目を明示
- 保存ボタンまわりを統一

## Phase 25.5：関係性に年表データ追加

関係性登録・編集フォームに、年表データを追加する。

### 仕様

関係性ごとに、最大10件まで時期と内容を登録できる。

### 入力項目

- 時期
- 内容

### 例

- 時期：5歳の頃
- 内容：出会う

### 保存形式

`original_character_relationships.timeline_items` にJSONで保存する。

### 用途

- 関係性詳細画面で表示
- 関係性一覧で年表件数を表示
- プロンプト作成時の関係性情報に反映

## Phase 25.6：関係性年表の追加ボタン化とプロンプト反映チェック

関係性フォームの年表データ入力欄を、最初3行表示・最大10行まで追加できるUIに変更する。

### 関係性年表

- 最初は3行のみ表示
- 「行を追加」で最大10行まで表示
- 保存済みデータが4件以上ある場合は、保存済み件数分を初期表示
- 各行にクリアボタンを設置

### プロンプト作成

プロンプト作成・編集フォームに「関係性の年表データもプロンプトに反映する」チェックボックスを追加する。

- チェックあり：選択キャラクター同士の関係性年表をプロンプト本文に含める
- チェックなし：年表データはプロンプト本文に含めない

### DB

`saved_prompts.include_relationship_timeline` を追加する。

## Phase 26：プロンプト作成フォームの最終整理

プロンプト作成・編集フォームをSTEP形式で再整理する。

### 調整内容

- STEP 1：基本情報
- STEP 2：登場人物
- STEP 3：作風・ジャンル
- STEP 4：あらすじ・構成
- STEP 5：プレビュー
- 関係性年表反映チェックを登場人物選択エリアに配置
- 作品キャラクターは選択作品に応じて表示
- 保存前プレビューとコピー機能を維持

## Phase 27：登録上限到達時の新規登録ブロック

一般会員が登録上限に達した場合、新規登録画面でフォームを表示せず、上限到達メッセージを表示する。

### 対象

- オリジナルキャラクター新規登録
- 関係性新規登録
- プロンプト新規作成

### 表示内容

- 現在の登録数 / 上限数
- 上限に達している説明
- 一覧へ戻るボタン

### 例

- オリジナルキャラクター：30 / 30
- 関係性：100 / 100
- プロンプト管理：50 / 50

最高管理者は上限なしのため、通常どおりフォームを表示する。
